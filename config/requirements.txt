streamlit==1.29.0
pandas
plotly
Pillow
opencv-python
PyYAML
# rosbag  # ROS依赖，需要单独安装ROS环境
# rospy   # ROS依赖，需要单独安装ROS环境
# Open3D - 注意：Python 3.13 暂不支持，建议使用 Python 3.8-3.11
# 临时解决方案：可以跳过此依赖，程序会自动降级功能
open3d>=0.15.0  # 需要 Python 3.8-3.11
matplotlib
laspy
numpy